# svhn-cnn-classifier
# SVHN Digit Classification with MLP and CNN

This project implements and compares an MLP and a CNN for digit classification on the Street View House Numbers (SVHN) dataset.

## What’s inside
- Data preprocessing and normalization
- MLP baseline model
- CNN model with improved generalization
- Training curves and test-set evaluation
- Saved checkpoints and qualitative prediction examples

## Files
- `svhn_cnn_classifier.ipynb`: full workflow notebook
- `requirements.txt`: minimal dependencies

